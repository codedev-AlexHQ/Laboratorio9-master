@model WebApplication1.Models.ProductModel

@{
    ViewBag.Title = "Eliminar Producto";
}

<h2>Eliminar Producto</h2>

<h3>�Seguro que quieres eliminar este producto?</h3>

<dl class="dl-horizontal">
    <dt>@Html.DisplayNameFor(m => m.nombreProducto)</dt>
    <dd>@Html.DisplayFor(m => m.nombreProducto)</dd>

    <dt>@Html.DisplayNameFor(m => m.precioUnitario)</dt>
    <dd>@Html.DisplayFor(m => m.precioUnitario)</dd>

    <dt>@Html.DisplayNameFor(m => m.cantidadStock)</dt>
    <dd>@Html.DisplayFor(m => m.cantidadStock)</dd>

    <dt>@Html.DisplayNameFor(m => m.categoriaProducto)</dt>
    <dd>@Html.DisplayFor(m => m.categoriaProducto)</dd>
</dl>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(m => m.id)
    <button type="submit" class="btn btn-danger">Eliminar</button>
    @Html.ActionLink("Back to List", "Index", null, new { @class = "btn btn-default" })
}
